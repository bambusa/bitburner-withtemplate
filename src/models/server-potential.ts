export class ServerPotential {
    potential:number;
    hostname:string;
    constructor(potential:number,hostname:string) {
        this.potential = potential;
        this.hostname = hostname;
    }
}